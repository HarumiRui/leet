задача 2

разворот 32 бит целого числа

1)число распадается на цифры через остаток и целочисленное деление 

2)сложность в том чтобы не использовать 64 битные типы при сборке ответа поэтому нужно проверять на переполнение

3)если число уже больше верхней границы деленной на 10 или равно ей то при след добавлении числа к результату произойдет переполнение int и поэтому мы останавливаем алгоритм и возвращаем 0

4) с нижней границей такая же история 

5) ну и сборка результата умножается на 10 "сдвигаем" по разрядам вверх и прибавляем след цифру

сложность O(1) так как int 32 бит содержит не более 100 цифр по которым идет алгоритм